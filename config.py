#v0.6

import os

class Config(object):
    # the first part of the command you use to call spc
    #base_cmd = 'docker exec scprime02 spc'
    base_cmd = 'spc'
    base2_cmd = 'spc -a localhost:4290'
